<!DOCTYPE html>
<html>
  <head>
    <title><%= document_title %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/cssbootstrap.css">
    <%= stylesheet_link_tag 'user', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="container text-body">
    <div id="wrapper">
      <%= render "shared/header" %>
      <%= render "shared/headsearch" %>
      <div id="container">
        <%= yield %>
      </div>
      <%= render "shared/footlinks" %>
      <%= render "shared/lightninglinks" %>
      <%= render "shared/footer" %>
    </div>
  </body>
</html>
